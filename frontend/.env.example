# LiveKit connection settings
# For local dev: ws://localhost:7880
# For Docker: ws://livekit:7880 (internal network)
# For distributed (remote GPU): wss://gpu-server.tailnet.ts.net:7443
LIVEKIT_URL=ws://localhost:7880

# LiveKit API credentials (use defaults for dev mode)
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# Browser WebSocket URL
# Set to 'auto' for LAN access (derives URL from browser's host)
# Set to explicit URL for specific configuration
# Examples:
#   auto                              - Dynamic detection from request host
#   ws://localhost:7880               - Local development
#   ws://192.168.1.100:7880           - Explicit LAN IP
#   wss://gpu-server.tailnet.ts.net:7443 - Distributed mode (remote GPU via Tailscale)
NEXT_PUBLIC_LIVEKIT_URL=auto

# Wake Word Detection (Picovoice Porcupine)
# Get your access key from https://console.picovoice.ai/
# Leave empty to disable wake word feature
NEXT_PUBLIC_PORCUPINE_ACCESS_KEY=

# Backend webhook server URL
# For Docker: http://agent:8889
# For distributed: https://gpu-server.tailnet.ts.net/api
WEBHOOK_URL=http://agent:8889

# Tool Registry source
# Set to 'true' to load from local caal-tools directory (for development)
# Leave empty or 'false' to fetch from GitHub (production)
TOOLS_REGISTRY_LOCAL=
